# Plan: Migration from JSON to PostgreSQL with Raw SQL

## Goal
1. Create `backend/TASK.md` — task description, subtasks, theory
2. Create `backend/DATA_FLOW.md` — backend-DB interaction flow
3. (Implementation in future phases — not this plan)

---

## Step 1: Create `backend/TASK.md`

Content:

```markdown
# TASK: Migration from JSON to PostgreSQL

## Назначение

Перенести хранилище данных бекенда фитнес-приложения с JSON-файлов на реляционную базу данных PostgreSQL. Обеспечить высокую производительность чтения за счёт индексирования справочных таблиц и использования оптимизированных SQL-функций. Настроить раздачу медиа-файлов (GIF) как публичных статических файлов сервера.

---

## Общее описание задачи

### Текущее состояние
- Все данные хранятся в `backend/data/*.json` (~93 000 строк упражнений + справочники).
- Каждый модуль (`exercises`, `muscles`, `bodyparts`, `equipments`, `contraindications`, `users`, `workout-templates`) содержит `*-json.repository.ts`, читающий JSON через `fs.readFileSync`.
- Связи между сущностями реализованы через slug-строки, резолвинг происходит в `ExercisesService.toResponseDto()`.
- Медиа-файлы (≈1500 GIF) лежат в `backend/media/`, но не раздаются сервером.

### Целевое состояние
- PostgreSQL хранит все данные в нормализованных таблицах.
- Связи реализованы через foreign key / junction-таблицы.
- Доступ к данным — через параметризованные SQL-запросы (без ORM).
- Справочные таблицы (`exercises`, `equipments`, `bodyparts`, `muscles`, `contraindications`) и их связи максимально проиндексированы.
- `backend/media/` раздаётся как статика по маршруту `/media`.

---

## Теория реализации

### 1. Соединение с PostgreSQL

Используется пакет `pg` (node-postgres) — нативный PostgreSQL-клиент для Node.js. Создаётся connection pool (`pg.Pool`), управляемый через NestJS-модуль `DatabaseModule` / `DatabaseService`.

**Паттерн:**
```
DatabaseModule (провайдит DatabaseService)
  → DatabaseService (держит pg.Pool)
    → query(sql, params) → pg.Pool.query()
```

Все SQL-репозитории инжектируют `DatabaseService` и вызывают `this.db.query(...)`.

### 2. Схема БД (таблицы и индексы)

#### Справочные таблицы (read-heavy, индексируются)

```sql
-- Базовые справочники
CREATE TABLE bodyparts  (id SERIAL PRIMARY KEY, name TEXT NOT NULL, slug TEXT UNIQUE NOT NULL);
CREATE TABLE equipments (id SERIAL PRIMARY KEY, name TEXT NOT NULL, slug TEXT UNIQUE NOT NULL);
CREATE TABLE muscles    (id SERIAL PRIMARY KEY, name TEXT NOT NULL, slug TEXT UNIQUE NOT NULL);
CREATE TABLE contraindications (id SERIAL PRIMARY KEY, name TEXT NOT NULL, slug TEXT UNIQUE NOT NULL);

-- Связь muscle ↔ muscle (антагонисты)
CREATE TABLE muscle_antagonists (
  muscle_id INT REFERENCES muscles(id),
  antagonist_id INT REFERENCES muscles(id),
  PRIMARY KEY (muscle_id, antagonist_id)
);

-- Упражнения (основная таблица)
CREATE TABLE exercises (
  id SERIAL PRIMARY KEY,
  exercise_id TEXT UNIQUE NOT NULL,
  name TEXT NOT NULL,
  slug TEXT UNIQUE NOT NULL,
  gif_url TEXT NOT NULL,
  instructions TEXT[] NOT NULL DEFAULT '{}',
  alias TEXT[] DEFAULT '{}',
  exercise_type TEXT,
  description TEXT,
  confidence NUMERIC(3,2),
  difficulty TEXT,
  movement_pattern TEXT,
  variations TEXT[] DEFAULT '{}'
);

-- Junction-таблицы (связи M:N)
CREATE TABLE exercise_target_muscles (
  exercise_id INT REFERENCES exercises(id) ON DELETE CASCADE,
  muscle_id INT REFERENCES muscles(id),
  PRIMARY KEY (exercise_id, muscle_id)
);
CREATE TABLE exercise_secondary_muscles (
  exercise_id INT REFERENCES exercises(id) ON DELETE CASCADE,
  muscle_id INT REFERENCES muscles(id),
  PRIMARY KEY (exercise_id, muscle_id)
);
CREATE TABLE exercise_body_parts (
  exercise_id INT REFERENCES exercises(id) ON DELETE CASCADE,
  bodypart_id INT REFERENCES bodyparts(id),
  PRIMARY KEY (exercise_id, bodypart_id)
);
CREATE TABLE exercise_equipments (
  exercise_id INT REFERENCES exercises(id) ON DELETE CASCADE,
  equipment_id INT REFERENCES equipments(id),
  PRIMARY KEY (exercise_id, equipment_id)
);
CREATE TABLE exercise_contraindications (
  exercise_id INT REFERENCES exercises(id) ON DELETE CASCADE,
  contraindication_id INT REFERENCES contraindications(id),
  severity TEXT NOT NULL CHECK (severity IN ('forbidden', 'not_recommended', 'low_weight')),
  PRIMARY KEY (exercise_id, contraindication_id)
);
```

**Индексы:**
- `slug` на каждой справочной таблице — `UNIQUE` constraint автоматически создаёт B-tree индекс
- `exercises.exercise_id` — `UNIQUE` индекс
- B-tree индексы на junction-таблицах: `CREATE INDEX idx_etm_muscle ON exercise_target_muscles(muscle_id)` и аналогичные для быстрого обратного поиска
- `exercises.name` — индекс для поиска (`ILIKE`), возможно GIN с `gin_trgm_ops`
- `exercises.slug` — `UNIQUE` индекс

#### Пользовательские таблицы (read-write)

```sql
CREATE TABLE users (
  id TEXT PRIMARY KEY,
  device_id TEXT UNIQUE NOT NULL,
  name TEXT,
  weight NUMERIC(5,1) CHECK (weight BETWEEN 20 AND 300),
  height NUMERIC(5,1) CHECK (height BETWEEN 50 AND 300),
  age INT CHECK (age BETWEEN 10 AND 120),
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE user_contraindications (
  user_id TEXT REFERENCES users(id) ON DELETE CASCADE,
  contraindication_id INT REFERENCES contraindications(id),
  PRIMARY KEY (user_id, contraindication_id)
);
```

#### Workout templates (read-write)

```sql
CREATE TABLE workout_templates (
  id TEXT PRIMARY KEY,
  user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  description TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE workout_exercises (
  id SERIAL PRIMARY KEY,
  template_id TEXT NOT NULL REFERENCES workout_templates(id) ON DELETE CASCADE,
  exercise_slug TEXT NOT NULL,
  sets INT NOT NULL,
  reps INT,
  rest_between_sets INT,
  rest_after_exercise INT,
  sort_order INT NOT NULL
);

CREATE TABLE scheduled_workouts (
  id TEXT PRIMARY KEY,
  template_id TEXT NOT NULL REFERENCES workout_templates(id) ON DELETE CASCADE,
  user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  day_of_week TEXT NOT NULL,
  time TEXT NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

### 3. Оптимизированные SQL-функции

Вместо ORM-запросов, пишем SQL-функции в PostgreSQL:

| Функция | Описание |
|---------|----------|
| `find_exercises_paginated(p_limit, p_offset, p_equipments, p_muscles, p_contraindications, p_search)` | Пагинированный поиск с фильтрацией через JOIN по junction-таблицам |
| `find_similar_exercises(p_slug, p_limit, p_offset)` | Упражнения с теми же целевыми мышцами (исключая текущее) |
| `find_antagonist_exercises(p_slug, p_antagonist_muscle_ids, p_limit, p_offset)` | Упражнения для мышц-антагонистов |
| `find_exercise_full_by_slug(p_slug)` | Полная загрузка упражнения со всеми связями через `json_agg()` / `json_build_object()` |

Пример функции для получения упражнения со связями:
```sql
CREATE OR REPLACE FUNCTION find_exercise_full_by_slug(p_slug TEXT)
RETURNS JSONB AS $$
SELECT jsonb_build_object(
  'exerciseId', e.exercise_id,
  'name', e.name,
  'slug', e.slug,
  'gifUrl', e.gif_url,
  'instructions', e.instructions,
  'alias', e.alias,
  'exerciseType', e.exercise_type,
  'description', e.description,
  'confidence', e.confidence,
  'difficulty', e.difficulty,
  'movementPattern', e.movement_pattern,
  'variations', e.variations,
  'targetMuscles', (SELECT json_agg(json_build_object('name', m.name, 'slug', m.slug))
    FROM exercise_target_muscles etm JOIN muscles m ON m.id = etm.muscle_id
    WHERE etm.exercise_id = e.id),
  'secondaryMuscles', (SELECT json_agg(json_build_object('name', m.name, 'slug', m.slug))
    FROM exercise_secondary_muscles esm JOIN muscles m ON m.id = esm.muscle_id
    WHERE esm.exercise_id = e.id),
  'bodyParts', (SELECT json_agg(json_build_object('name', bp.name, 'slug', bp.slug))
    FROM exercise_body_parts ebp JOIN bodyparts bp ON bp.id = ebp.bodypart_id
    WHERE ebp.exercise_id = e.id),
  'equipments', (SELECT json_agg(json_build_object('name', eq.name, 'slug', eq.slug))
    FROM exercise_equipments ee JOIN equipments eq ON eq.id = ee.equipment_id
    WHERE ee.exercise_id = e.id),
  'contraindications', (SELECT json_agg(json_build_object('slug', c.slug, 'severity', ec.severity))
    FROM exercise_contraindications ec JOIN contraindications c ON c.id = ec.contraindication_id
    WHERE ec.exercise_id = e.id)
)
FROM exercises e
WHERE e.slug = p_slug;
$$ LANGUAGE sql STABLE;
```

### 4. Статические файлы (media)

В `main.ts` добавляется:
```typescript
app.use('/media', express.static(path.join(__dirname, '..', 'media')));
```
Клиенты получают GIF по URL `/media/trmte8s.gif`.

---

## Рабочие подзадачи

### Подзадача 1: Инфраструктура подключения к БД
- Установить `pg` и `@types/pg`
- Создать `DatabaseModule` / `DatabaseService` с connection pool
- Настроить переменные окружения (`DATABASE_URL`)

### Подзадача 2: SQL-схема и сидинг
- Создать `backend/sql/schema.sql` с DDL (таблицы, индексы, функции)
- Создать seed-скрипт `backend/sql/seed.ts`, загружающий данные из JSON в PostgreSQL
- Проверить корректность миграции (сравнить количество записей)

### Подзадача 3: SQL-репозитории (справочники)
- `MusclesSqlRepository` — `findAll()`, `findBySlug()`, `findAntagonists()`
- `BodypartsSqlRepository` — `findAll()`
- `EquipmentsSqlRepository` — `findAll()`
- `ContraindicationsSqlRepository` — `findAll()`, `findBySlug()`

### Подзадача 4: SQL-репозитории (упражнения)
- `ExercisesSqlRepository` — `findPaginated()`, `findBySlug()`, `findSimilar()`, `findAntagonist()`
- Сложные JOIN-запросы по junction-таблицам
- Оптимизация: SQL-функции с `json_agg()` для загрузки упражнения с одной DB-roundtrip

### Подзадача 5: SQL-репозитории (пользователи)
- `UsersSqlRepository` — `findById()`, `findByDeviceId()`, `create()`, `update()`

### Подзадача 6: SQL-репозитории (тренировки)
- `WorkoutTemplatesSqlRepository` — CRUD для шаблонов
- `ScheduledWorkoutsSqlRepository` — CRUD для расписания
- Транзакции для создания/обновления с упражнениями

### Подзадача 7: Обновление модулей
- Заменить провайдеры во всех `*.module.ts` файлах (Json → Sql)
- Упростить `ExercisesService.toResponseDto()` — slug-резолвинг больше не нужен, SQL-запросы возвращают полные объекты

### Подзадача 8: Статические файлы media
- Добавить `express.static` в `main.ts` для маршрута `/media`
- Обновить `gifUrl` в данных или добавить трансформацию в DTO

### Подзадача 9: Тестирование и cleanup
- Протестировать все API endpoints
- Удалить старые `*-json.repository.ts` файлы
- Удалить `backend/data/*.json` (опционально, оставить как backup)

---

## Ограничения и решения

| Проблема | Решение |
|----------|---------|
| Без ORM → много ручного SQL | Параметризованные запросы через `DatabaseService.query()` |
| Синхронные репозитории → асинхронные | Все методы репозиториев станут `async`/`Promise` |
| N+1 при загрузке связей упражнения | SQL-функции с `json_agg()` / `json_build_object()` — один запрос |
| Транзакционность при создании шаблонов | `DatabaseService.transaction()` с `BEGIN/COMMIT/ROLLBACK` |
```

---

## Step 2: Create `backend/DATA_FLOW.md`

Content:

```markdown
# DATA FLOW: Backend ↔ PostgreSQL

## Общая архитектура

```
┌──────────────────────────────────────────────────────────────┐
│                        NestJS Backend                        │
│                                                              │
│  ┌──────────┐   ┌───────────────┐   ┌─────────────────────┐ │
│  │Controller│──▶│    Service     │──▶│  SQL Repository     │ │
│  │  (HTTP)  │   │ (business logic)│   │ (*-sql.repository)  │ │
│  └──────────┘   └───────────────┘   └──────────┬──────────┘ │
│                                                  │            │
│                                        ┌─────────▼─────────┐ │
│                                        │  DatabaseService   │ │
│                                        │   (pg.Pool query)  │ │
│                                        └─────────┬─────────┘ │
└──────────────────────────────────────────────────┼───────────┘
                                                   │ TCP/SQL
                                         ┌─────────▼─────────┐
                                         │    PostgreSQL      │
                                         │   (tables, funcs)  │
                                         └───────────────────┘
```

---

## Слой подключения: DatabaseModule / DatabaseService

### DatabaseModule
- Глобальный NestJS-модуль (`@Global()`)
- Провайдит `DatabaseService` как singleton
- Читает конфиг из переменных окружения (`DATABASE_URL`)

### DatabaseService
- Держит `pg.Pool` (connection pool)
- Экспортирует методы:
  - `query<T>(sql: string, params?: unknown[]): Promise<T[]>` — выполнение SQL
  - `transaction<T>(fn: (client: pg.PoolClient) => Promise<T>): Promise<T>` — выполнение в транзакции

---

## Потоки данных по модулям

### 1. Справочники (Read-Only из БД)

#### Bodyparts / Equipments / Contraindications / Muscles

```
Client ──GET /muscles──▶ MusclesController
                              │
                         MusclesService
                              │
                    MusclesSqlRepository
                              │
                    ──SELECT * FROM muscles──▶ PostgreSQL
                              │
                    ◀── Muscle[] ──────────────
```

SQL-запрос (пример для muscles):
```sql
SELECT id, name, slug FROM muscles ORDER BY name;
```

#### Muscles: поиск антагонистов

```
Client ──GET /exercises/:slug/antagonist──▶ ExercisesController
                                                  │
                                           ExercisesService
                                           │              │
                         ExercisesSqlRepo  │              │ MusclesSqlRepo
                              │            │              │      │
                    ── findAntagonists(slug) ────────────────────▶ PostgreSQL
                              │                                       │
                              │            SELECT m.* FROM muscles m  │
                              │            JOIN muscle_antagonists ma │
                              │            ON m.id = ma.antagonist_id│
                              │            WHERE ma.muscle_id = $1   │
                              │                                       │
                              ◀──────────────────────────────────────
```

### 2. Упражнения (Read-Only, фильтрация)

#### Пагинированный поиск с фильтрами

```
Client ──GET /exercises?equipments=band,barbell&targetMuscles=traps&search=shrug──▶ Controller
          │
          │ { equipments, targetMuscles, contraindications, search, page, limit }
          ▼
     ExercisesService.findAll(page, limit, filters)
          │
          ▼
     ExercisesSqlRepository.findPaginated(page, limit, filters)
          │
          ▼ PostgreSQL
```

SQL (динамически строится в зависимости от фильтров):

```sql
-- Шаг 1: Подсчёт общего количества
SELECT COUNT(DISTINCT e.id) AS total
FROM exercises e
WHERE 1=1
  AND EXISTS (
    SELECT 1 FROM exercise_equipments ee
    JOIN equipments eq ON eq.id = ee.equipment_id
    WHERE ee.exercise_id = e.id AND eq.slug = ANY($1::text[])
  )
  AND NOT EXISTS (
    SELECT 1 FROM exercise_contraindications ec
    JOIN contraindications c ON c.id = ec.contraindication_id
    WHERE ec.exercise_id = e.id AND c.slug = ANY($2::text[])
  )
  AND e.name ILIKE '%' || $3 || '%';

-- Шаг 2: Получение страницы данных
SELECT e.*, 
  (SELECT json_agg(jsonb_build_object('name', m.name, 'slug', m.slug))
   FROM exercise_target_muscles etm JOIN muscles m ON m.id = etm.muscle_id
   WHERE etm.exercise_id = e.id) AS target_muscles,
  -- ... аналогично для secondary_muscles, body_parts, equipments, contraindications
FROM exercises e
WHERE /* те же фильтры */
ORDER BY e.name
LIMIT $4 OFFSET $5;
```

#### Поиск похожих упражнений

```sql
SELECT e.*, /* + агрегация связей */
FROM exercises e
WHERE e.slug != $1
  AND EXISTS (
    SELECT 1 FROM exercise_target_muscles etm
    WHERE etm.exercise_id = e.id
    AND etm.muscle_id = ANY(
      SELECT etm2.muscle_id FROM exercise_target_muscles etm2
      WHERE etm2.exercise_id = (SELECT id FROM exercises WHERE slug = $1)
    )
  )
ORDER BY e.name
LIMIT $2 OFFSET $3;
```

### 3. Пользователи (Read-Write)

#### Создание пользователя (при авторизации)

```
Client ──POST /auth/login {deviceId}──▶ AuthController
                                           │
                                      AuthService.authenticate(deviceId)
                                           │
                                      ─── UsersSqlRepository.findByDeviceId(deviceId) ──▶ PG
                                           │
                                      ┌────┴─────┐
                                      │ Найден?  │
                                      └────┬─────┘
                                       Да │    │ Нет
                                          │    ▼
                                          │  UsersSqlRepository.create(deviceId)
                                          │    ──INSERT INTO users...──▶ PG
                                          ▼         ◀── returning user ──
                                     JwtService.sign({sub: user.id})
                                           │
                                      ◀── { accessToken, user }
```

SQL:
```sql
-- findByDeviceId
SELECT id, device_id, name, weight, height, age, created_at
FROM users WHERE device_id = $1;

-- create
INSERT INTO users (id, device_id, created_at)
VALUES ($1, $2, NOW())
RETURNING *;

-- update
UPDATE users SET name = $2, weight = $3, height = $4, age = $5
WHERE id = $1
RETURNING *;
```

#### Обновление профиля с противопоказаниями (транзакция)

```sql
BEGIN;
  UPDATE users SET name = $2, weight = $3, height = $4, age = $5 WHERE id = $1;
  DELETE FROM user_contraindications WHERE user_id = $1;
  INSERT INTO user_contraindications (user_id, contraindication_id)
    SELECT $1, id FROM contraindications WHERE slug = ANY($6::text[]);
COMMIT;
```

### 4. Workout Templates (Read-Write, транзакции)

#### Создание шаблона тренировки

```
Client ──POST /workout-templates {name, exercises[]}──▶ Controller (JWT Guard)
                                                            │
                                                    WorkoutTemplatesService.create()
                                                            │
                                                    ─── DB.transaction() ──▶ PG

  BEGIN;
    INSERT INTO workout_templates (id, user_id, name, description)
    VALUES ($1, $2, $3, $4);

    INSERT INTO workout_exercises (template_id, exercise_slug, sets, reps, rest_between_sets, rest_after_exercise, sort_order)
    VALUES ($1, $5, $6, $7, $8, $9, $10), ...;
  COMMIT;
```

#### Получение расписания

```sql
SELECT sw.*, wt.name AS template_name
FROM scheduled_workouts sw
JOIN workout_templates wt ON wt.id = sw.template_id
WHERE sw.user_id = $1
ORDER BY sw.day_of_week, sw.time;
```

---

## Среда выполнения: переменные окружения

```env
DATABASE_URL=postgresql://user:password@localhost:5432/fitness_app
# или раздельные:
PGHOST=localhost
PGPORT=5432
PGUSER=fitness_user
PGPASSWORD=secret
PGDATABASE=fitness_app

# Медиа
MEDIA_PATH=./media      # путь к GIF-файлам (по умолчанию backend/media)
```

---

## Пул соединений

```
┌─────────────────────────────────┐
│         pg.Pool                 │
│  max: 20 connections            │
│  idleTimeoutMillis: 30000       │
│  connectionTimeoutMillis: 2000  │
│                                 │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐  │
│  │ c1 │ │ c2 │ │ c3 │ │... │  │
│  └────┘ └────┘ └────┘ └────┘  │
└─────────────────────────────────┘
        ▲           ▼
   DatabaseService.query(sql, params)
        ▲           ▼
   *SqlRepository methods
```

- Каждый вызов `query()` берёт соединение из pool и возвращает обратно
- Для транзакций — `pool.connect()` захватывает одно соединение на всю транзакцию

---

## Статические файлы: media

```
Client ──GET /media/trmte8s.gif──▶ NestJS (express.static middleware)
                                       │
                                  ┌────▼─────────────┐
                                  │ backend/media/    │
                                  │   trmte8s.gif     │
                                  │   LMGXZn8.gif     │
                                  │   ... (~1500)     │
                                  └──────────────────┘
```

Конфигурация в `main.ts`:
```typescript
import * as path from 'path';

// Перед app.listen():
app.use('/media', express.static(path.join(__dirname, '..', 'media')));
```

Поле `gifUrl` в ответе API содержит относительный путь `/media/{exerciseId}.gif`.
```

---

## Summary of implementation steps (future phases, NOT in this plan)

1. Install `pg` + `@types/pg`
2. Create `DatabaseModule` / `DatabaseService`
3. Create `backend/sql/schema.sql` + seed script
4. Create `*-sql.repository.ts` for all 7 modules
5. Update all `*.module.ts` to use SQL repos
6. Update `ExercisesService` to remove slug-resolution
7. Add `express.static` for `/media` in `main.ts`
8. Test & cleanup old JSON repos
